#
# build.properties
#

# Ant properties file for OME fork of Apache POI.
# Download Apache Ant from http://ant.apache.org/.
# Type "ant -p" for a list of targets.

component.name           = ome-poi
component.jar            = ome-poi.jar
component.version        = 1.0.0
component.classpath      = ${artifact.dir}/formats-common.jar:\
                           ${lib.dir}/commons-logging.jar
component.java-version   = 1.7
component.deprecation    = false

component.resources-bin  =
component.resources-text =

component.main-class     =
component.runtime-cp     = ${component.classpath}
