#!/bin/bash

# bfconvert: a script for converting image files between formats

# Required JARs: loci_tools.jar

# JAR libraries must be in the same directory as this
# command line script for the command to function.

DIR=`dirname "$0"`

PROG=loci.formats.tools.ImageConverter "$DIR/launch.sh" "$@"
