# Template designed specifically for viewing (not editing) metadata.

default-width "600"
default-height "500"

editable "false"
prefer-companion "true"
edit-template-fields "false"
edit-mapping "false"

tab {
  name "Image Information"

  # demonstrates how to set up an arbitrarily repeated group
  # this will show information for every Image in the open file

  group {
    name "Image"

    field {
      name "Name"
      type "var"
      map "Image:Name"
    }

    field {
      name "Creation Date"
      type "var"
      map "Image:CreationDate"
    }

    field {
      name "Description"
      type "var"
      map "Image:Description"
    }

    field {
      name "Width"
      type "int"
      map "Image:Pixels:SizeX"
    }
  
    field {
      name "Height"
      type "int"
      map "Image:Pixels:SizeY"
    }
 
    field {
      name "Number of slices"
      type "int"
      map "Image:Pixels:SizeZ"
    }

    field {
      name "Number of channels"
      type "int"
      map "Image:Pixels:SizeC"
    }

    field {
      name "Number of timepoints"
      type "int"
      map "Image:Pixels:SizeT"
    }
  
    field {
      name "Big Endian"
      type "bool"
      default "false"
      map "Image:Pixels:BigEndian"
    }

    field {
      name "Pixel Type"
      type "var"
      map "Image:Pixels:PixelType"
    }
  }
}

tab {

  name "Experimenter Information"

  field {
    name "First Name"
    type "var"
    map "CustomAttributes:Experimenter:FirstName"
  }

  field {
    name "Last Name"
    type "var"
    map "CustomAttributes:Experimenter:LastName"
  }

  field {
    name "Email"
    type "var"
    span "1,3" 
    map "CustomAttributes:Experimenter:Email"
  }
}

tab {
  name "Metadata hashtable"

  grid "1,2"

  field {
    name "Field name"
    map "Image:CustomAttributes:OriginalMetadata:name"
    repeated "true" 
    type "var" 
    grid "1,1" 
  }

  field {
    name "Field value"
    map "Image:CustomAttributes:OriginalMetadata:value"
    repeated "true"
    type "var"
    grid "1,3" 
  }

}
